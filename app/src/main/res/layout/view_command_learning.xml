<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- メインコンテンツ -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="#111418">

        <!-- 入力エリア -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:paddingTop="10dp"
            android:paddingBottom="6dp"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/inputDisplay"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:hint="enter command text..."
                android:textColorHint="#8B949E"
                android:textColor="#E6EDF3"
                android:textSize="14sp"
                android:background="@drawable/bg_input_field"
                android:padding="10dp"
                android:minHeight="38dp" />

            <TextView
                android:id="@+id/addButton"
                android:layout_width="wrap_content"
                android:layout_height="38dp"
                android:text="+ ADD"
                android:textColor="#6BA4FF"
                android:textSize="11sp"
                android:letterSpacing="0.15"
                android:gravity="center"
                android:background="@drawable/btn_outline_accent"
                android:paddingStart="14dp"
                android:paddingEnd="14dp"
                android:layout_marginStart="8dp" />

        </LinearLayout>

        <!-- コマンド一覧 (スクロール可能) -->
        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:clipToPadding="false">

            <LinearLayout
                android:id="@+id/commandList"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical" />

        </ScrollView>

    </LinearLayout>

    <!-- キーボードエリア (デフォルト非表示, スライドイン) -->
    <LinearLayout
        android:id="@+id/keyboardContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:visibility="gone">

        <!-- scrim背景 -->
        <View
            android:id="@+id/keyboardScrim"
            android:layout_width="match_parent"
            android:layout_height="24dp"
            android:background="@drawable/bg_keyboard_scrim" />

        <!-- キーボード -->
        <com.example.voiceinput.AlphanumericKeyboardView
            android:id="@+id/alphaKeyboard"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#111418"
            android:padding="2dp" />

    </LinearLayout>

</FrameLayout>
